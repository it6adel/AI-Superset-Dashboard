"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = __importDefault(require("fs"));
const process_1 = require("process");
const util_1 = require("./lib/util");
const parentPid = parseInt(process.argv[2], 10);
const tmpTsconfig = process.argv[3];
// Check every second if the parent process is still running
setInterval(() => {
    if (!(0, util_1.isRunning)(parentPid)) {
        if (fs_1.default.existsSync(tmpTsconfig)) {
            fs_1.default.unlinkSync(tmpTsconfig);
        }
        (0, process_1.exit)(0);
    }
}, 1000);
// Ensure the process exits after 1 minute
setTimeout(() => {
    (0, process_1.exit)(0);
}, 60000);
//# sourceMappingURL=cleanupMonitor.js.map