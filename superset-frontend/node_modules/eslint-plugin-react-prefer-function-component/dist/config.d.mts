import type { ESLint } from "eslint";
declare const config: {
    configs: {
        recommended: {
            plugins: {
                "prefer-function-component": ESLint.Plugin;
            };
            rules: {
                "prefer-function-component/prefer-function-component": "error";
            };
        };
    };
};
export default config;
